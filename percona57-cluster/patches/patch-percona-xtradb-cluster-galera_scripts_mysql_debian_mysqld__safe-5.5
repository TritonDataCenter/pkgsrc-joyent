$NetBSD$

Portability.

--- percona-xtradb-cluster-galera/scripts/mysql/debian/mysqld_safe-5.5.orig	2017-04-12 10:07:25.000000000 +0000
+++ percona-xtradb-cluster-galera/scripts/mysql/debian/mysqld_safe-5.5
@@ -169,7 +169,7 @@ wsrep_pick_url() {
     nc -z "$host" $port >/dev/null && break
   done
 
-  if [ "$url" == "0" ]; then
+  if [ "$url" = "0" ]; then
     log_error "ERROR: none of the URLs in '$@' is reachable."
     return 1
   fi
