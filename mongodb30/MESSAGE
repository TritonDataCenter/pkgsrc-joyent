===========================================================================
$NetBSD$

MongoDB 3.0 and later no longer comes with the usual CLI tools like
mongodump and mongorestore, which have been moved to a separate repository
by upstream project maintainers.

Please install the mongo-tools package for these:

  pkgin -y in mongo-tools

===========================================================================
